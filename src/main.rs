extern crate bowling_scorer;

// use std::io;
// use std::io::prelude::*;


fn main() {

//    loop {
//        let stdin = io::stdin();
//        for line in stdin.lock().lines() {
//            println!("{}", line.unwrap());
//        }
//    }

}
